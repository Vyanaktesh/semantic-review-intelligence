# Override only if the Node.js server runs on a different host/port.
# In development the Vite proxy (vite.config.ts) routes /api → http://localhost:3001
# automatically, so this variable is NOT required for local development.
# VITE_API_BASE_URL=http://localhost:3001
